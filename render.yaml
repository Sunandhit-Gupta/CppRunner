services:
  - type: web
    name: cpp-compiler
    runtime: node
    buildCommand: |
      apt-get update
      apt-get install -y g++
      npm install
    startCommand: node server.js
    plan: free